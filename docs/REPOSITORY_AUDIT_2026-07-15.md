# ביקורת עומק לקלוטקורד — 2026-07-15

> מסמך ביקורת היסטורי. הוא אינו דף כללים; המצב החי נמצא ב־`CURRENT_STATE.md`.

## מטרת הביקורת
לבדוק מה נבנה בפועל במיזם **קלוטקורד**, האם הריפו המרכזי משקף את המציאות, ומהו הצעד הבטוח הבא.

## תמונת מצב עובדתית
### הריפו המרכזי
- כתובת נוכחית: `yanivmizrachiy/colors`.
- שם המיזם: **קלוטקורד**.
- ענף ראשי: `main`.
- Visibility: ציבורי.
- התוכן המקורי של פרויקט הצבעים שמור בענף `archive/colors-original-2026-07-14`.
- הריפו הוסב למרכז מחקר, תכנון וניהול העבודה עם Claude Code.

### מה כבר נוצר
1. **תשתית ניהול ומקור אמת**
   - `CLAUDE.md`, `CURRENT_STATE.md`, `PROJECT_GOALS.md`, `DECISIONS.md`, `RESEARCH_LOG.md` ו־`docs/ROADMAP.md`.
2. **מתודולוגיית מחקר**
   - תוכנית מחקר, מודל ראיות, רמות ודאות, שיטת חקירת ריפו ומודל דירוג.
3. **מחקר חוצה־פרויקטים**
   - דוחות על `mathmath`, `parabula-next`, `misparim`, `maagar`, `microsoft-forms`, `www` ו־`bbb`.
   - אינדקס ריפוים, מטריצת דפוסים ורשם הזדמנויות.
4. **תוכנית עבודה אישית**
   - מדיניות זיכרון, מודלים, effort, context, Subagents, MCP, Git/PR, בדיקות ו־release readiness.
5. **כלים יומיומיים**
   - מדריך יומי, task brief קצר ותבנית מדידת session.
6. **חבילת פיילוט**
   - תוכנית מדידה ל־`misparim`, תנאי הצלחה, תנאי עצירה, rollback וסדר עדיפויות.

## מצב הפיילוט בפועל
נוצר ב־`yanivmizrachiy/misparim`:
- ענף: `pilot/claude-memory-v1`.
- PR: `misparim#1`.
- בסיס ה־baseline הישן נשאר ב־`main` ב־SHA `ce020c8fcf026f0ba1909f5844a49ab7ac0aafe3`.
- מועמד השינוי נמצא ב־SHA `4c03a1cc02ebe72856a320ec7d71092e62d38bec`.
- 11 קבצים השתנו, כולם קובצי Markdown/זיכרון בלבד.
- אין שינוי בקוד מוצר, בתוכן מתמטי, ב־DB או ב־assets.
- סטטוס Vercel למועמד השינוי: success.
- ה־PR פתוח וניתן למיזוג, אך **לא מוזג**.

## אי־התאמות שנמצאו בביקורת
### 1. מצב מרכז השליטה התיישן
מספר מסמכים הציגו את שלב B כפעולה שלא התחילה, למרות שמועמד השינוי כבר היה מוכן ב־PR.

### 2. `CLAUDE.md` פיגר אחרי השלב האמיתי
הוא הגדיר את המיזם כ־"מחקר ותכנון בלבד", אף שבפועל החל פיילוט מבוקר בענף וב־PR.

### 3. baseline טרם נמדד
השינוי המוצע כבר נכתב, אך כל עוד `misparim/main` נשאר ללא שינוי ניתן לבצע baseline אמיתי על ה־SHA הישן. אסור לטעון לחיסכון או הצלחה לפני המדידה.

### 4. ריבוי מסמכים יצר סיכון ל־state drift
הארכיטקטורה הייתה טובה, אך אותו סטטוס תועד בכמה מקומות.

### 5. אין עדיין נתוני שימוש אמיתיים
קיימות תבניות, אך אין שלושה רישומי baseline ושלושה רישומי post-change.

## תיקונים שבוצעו לאחר הביקורת
- `CURRENT_STATE.md` נקבע כמקור המצב החי.
- `CLAUDE.md` נבנה כדף הכללים המחייב היחיד.
- `DECISIONS.md` סומן כהיסטוריית החלטות בלבד.
- שם המיזם תוקן ל־**קלוטקורד**.
- פרומפט ההפעלה הישן של מחשב הסלון הוסר.
- מסמך אבטחה כפול הוסר לאחר שכל החובות רוכזו ב־`CLAUDE.md`.
- README, Roadmap, יומן המחקר ותוכנית המחקר סונכרנו למבנה החדש.

## הערכת איכות
### חזק מאוד
- הפרדה בין מחקר, החלטות, מצב חי ויישום.
- דגש על מקור אמת, פרטיות ו־rollback.
- הבחנה בין PASS טכני, live verification ו־release readiness.
- task brief קצר ומעשי.
- החלטה לא להתקין Skills/Hooks/Agents לפני צורך מוכח.
- הפיילוט ב־`misparim` מבודד, הפיך ואינו משנה מוצר.

### עדיין דורש עבודה
- שלוש מדידות baseline לפני מיזוג.
- inventory מקומי מצומצם של Claude Code.
- מחקר ודירוג של Skills, Plugins, Hooks ו־MCP רלוונטיים.
- Rename של ה־slug מ־`colors` ל־`klotkord` דרך GitHub.
- בחינה עתידית של שינוי Visibility ל־Private.

## החלטה מומלצת
1. **לא למזג עדיין את `misparim#1`.**
2. לבצע שלוש משימות baseline אמיתיות על `misparim/main` ב־SHA הישן.
3. להשתמש באותו מודל וב־effort דומה בכל זוג השוואה.
4. לאחר baseline, לבצע review נוסף ולהחליט אם למזג.
5. לבצע שלוש משימות post-change.
6. להשוות זמן, context/usage, קבצים שנקראו, תיקונים, רגרסיות ומעורבות יניב.
7. לקבל החלטה: keep, adjust או revert.

## פרוטוקול מדידה
- כל משימה מתחילה ב־session חדש.
- baseline מופעל מתוך checkout של `main` ב־SHA הקבוע.
- post-change מופעל מתוך מועמד השינוי או לאחר merge.
- משתמשים במשימות דומות בגודל, תחום וסיכון — לא באותה משימה בדיוק.
- אין לשנות מודל/effort באמצע זוג השוואה.
- לא שומרים transcripts בריפו; רק מדדים מסוכמים.

## הצעד הבא
השלב הפעיל הוא ניקוי קלוטקורד, מחקר הרחבות עדכני והכנת שלוש משימות baseline אמיתיות.
