# ביקורת עומק לריפו `colors` — 2026-07-15

## מטרת הביקורת
לבדוק מה נבנה בפועל במיזם **קלוט־קוד**, האם הריפו המרכזי משקף את המציאות, ומהו הצעד הבטוח הבא.

## תמונת מצב עובדתית
### הריפו המרכזי
- ריפו: `yanivmizrachiy/colors`.
- ענף ראשי: `main`.
- Visibility: ציבורי.
- התוכן המקורי של פרויקט הצבעים שמור בענף `archive/colors-original-2026-07-14`.
- הריפו הוסב למרכז מחקר, תכנון וניהול העבודה עם Claude Code.

### מה כבר נוצר
1. **תשתית ניהול ומקור אמת**
   - `CLAUDE.md`, `CURRENT_STATE.md`, `PROJECT_GOALS.md`, `DECISIONS.md`, `RESEARCH_LOG.md` ו־`docs/ROADMAP.md`.
2. **מתודולוגיית מחקר**
   - תוכנית מחקר, מודל ראיות, רמות ודאות, שיטת חקירת ריפו ומודל דירוג.
3. **מחקר חוצה־פרויקטים**
   - דוחות על `mathmath`, `parabula-next`, `misparim`, `maagar`, `microsoft-forms`, `www` ו־`bbb`.
   - אינדקס ריפוים, מטריצת דפוסים ורשם הזדמנויות.
4. **תוכנית עבודה אישית**
   - מדיניות זיכרון, מודלים, effort, context, Subagents, MCP, Git/PR, בדיקות ו־release readiness.
5. **כלים יומיומיים**
   - מדריך יומי, task brief קצר ותבנית מדידת session.
6. **חבילת פיילוט**
   - תוכנית מדידה ל־`misparim`, תנאי הצלחה, תנאי עצירה, rollback וסדר עדיפויות.

## מצב הפיילוט בפועל
נוצר ב־`yanivmizrachiy/misparim`:
- ענף: `pilot/claude-memory-v1`.
- PR: `misparim#1`.
- בסיס ה־baseline הישן נשאר ב־`main` ב־SHA `ce020c8fcf026f0ba1909f5844a49ab7ac0aafe3`.
- מועמד השינוי נמצא ב־SHA `4c03a1cc02ebe72856a320ec7d71092e62d38bec`.
- 11 קבצים השתנו, כולם קובצי Markdown/זיכרון בלבד.
- אין שינוי בקוד מוצר, בתוכן מתמטי, ב־DB או ב־assets.
- סטטוס Vercel למועמד השינוי: success.
- ה־PR פתוח וניתן למיזוג, אך **לא מוזג**.

## אי־התאמות שנמצאו
### 1. מצב מרכז השליטה התיישן
`README`, `CURRENT_STATE`, `ROADMAP`, `DECISIONS` ו־Issue #12 עדיין מציגים את שלב B כפעולה שלא התחילה, למרות שמועמד השינוי כבר מוכן ב־PR.

### 2. `CLAUDE.md` של הריפו המרכזי מפגר אחרי השלב האמיתי
הוא עדיין מגדיר את המיזם כ־"מחקר ותכנון בלבד", בעוד שבפועל החל פיילוט מבוקר בריפו יעד — בענף וב־PR בלבד.

### 3. baseline טרם נמדד
השינוי המוצע כבר נכתב, אך כל עוד `misparim/main` נשאר ללא שינוי ניתן עדיין לבצע baseline אמיתי על ה־SHA הישן. אסור לטעון לחיסכון או הצלחה לפני המדידה.

### 4. ריבוי מסמכים יוצר סיכון ל־state drift
הארכיטקטורה טובה, אך אותו סטטוס מתועד בכמה מקומות. כאשר אירוע מתקדם במהירות, יש לעדכן לפחות:
- `CURRENT_STATE.md` — מקור המצב החי.
- `docs/ROADMAP.md` — מצב השלבים.
- Issue הביצוע — checklist תפעולי.
מסמכי מחקר והחלטות אינם צריכים לשכפל סטטוס זמני.

### 5. אין עדיין נתוני שימוש אמיתיים
קיימות תבניות מצוינות, אך אין שלושה רישומי baseline ואין שלושה רישומי post-change. לכן התוכנית מקצועית, אך טרם הוכחה אמפירית.

## הערכת איכות
### חזק מאוד
- הפרדה ברורה בין מחקר, החלטות, מצב חי ויישום.
- דגש עקבי על מקור אמת, פרטיות ו־rollback.
- הבחנה נכונה בין PASS טכני, live verification ו־release readiness.
- task brief קצר ומעשי.
- החלטה לא לבנות Skills/Hooks/Agents לפני צורך מוכח.
- הפיילוט ב־`misparim` מבודד, הפיך ואינו משנה מוצר.

### דורש תיקון
- סנכרון מצב אוטומטי/ידני בין הריפו המרכזי לריפו היעד.
- צמצום טענות מצב כפולות.
- מדידה לפני מיזוג.
- inventory מקומי מצומצם של Claude Code עדיין חסר.
- שם הריפו `colors` ו־Visibility ציבורי אינם אידאליים למרכז שליטה קבוע.

## החלטה מומלצת
1. **לא למזג עדיין את `misparim#1`.**
2. לבצע שלוש משימות baseline אמיתיות על `misparim/main` ב־SHA הישן.
3. להשתמש באותו מודל וב־effort דומה בכל זוג השוואה.
4. לאחר baseline, למזג את PR #1 רק אם אין בעיית review או חוזה מוצר שנפגע.
5. לבצע שלוש משימות post-change.
6. להשוות זמן, context/usage, קבצים שנקראו, תיקונים, רגרסיות ומעורבות יניב.
7. לקבל החלטה: keep, adjust או revert.

## פרוטוקול מדידה מתוקן
כדי למנוע הטיית זיכרון:
- כל משימה מתחילה ב־session חדש.
- baseline מופעל מתוך checkout של `main` ב־SHA הקבוע.
- post-change מופעל מתוך checkout של מועמד השינוי או לאחר merge.
- אין להשתמש באותה משימה בדיוק פעמיים; משתמשים במשימות דומות בגודל, תחום וסיכון.
- אין לשנות מודל/effort באמצע זוג השוואה.
- לא שומרים transcripts בריפו; רק מדדים מסוכמים.

## הצעד הבא
השלב הפעיל הוא **Baseline מול מועמד שינוי מוכן**. מרכז השליטה צריך לשקף זאת, ו־Issue #12 צריך להפוך מ־"ממתין לאישור" למסלול ניסוי פעיל שממתין למדידות לפני merge.
