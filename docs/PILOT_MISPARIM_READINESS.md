# חבילת פיילוט `misparim`

עודכן: 2026-07-15

## סטטוס נוכחי
הפיילוט עבר משלב מוכנות לשלב **baseline מול מועמד שינוי מוכן**:
- `misparim/main` נשאר ללא שינוי ב־SHA `ce020c8fcf026f0ba1909f5844a49ab7ac0aafe3`.
- מועמד השינוי הוכן בענף `pilot/claude-memory-v1` וב־PR #1.
- מועמד השינוי נמצא ב־SHA `4c03a1cc02ebe72856a320ec7d71092e62d38bec`.
- ה־PR פתוח, mergeable ו־Vercel success, אך **לא ממוזג**.
- השלב הבא הוא שלוש מדידות baseline על `main` הישן.

## מטרת הפיילוט
לבדוק באופן מדיד האם ארכיטקטורת זיכרון רזה ומדורגת משפרת את העבודה עם Claude Code בלי להוריד איכות, אמינות או המשכיות.

## ההשערה
הפחתת מידע שנטען בכל session, הפרדת state זמני והעברת שני תחומים ל־path-scoped rules צפויות:
- לצמצם context ושימוש במכסה.
- לקצר את זמן ההבנה הראשוני.
- להפחית חזרות בין קובצי זיכרון.
- לשמור או לשפר את שיעור ההצלחה בפעם הראשונה.

## גבולות קשיחים
- אין שינוי קוד מוצר בפיילוט הזיכרון הראשון.
- אין Skill, Hook, Agent או MCP חדש בשלב הראשון.
- אין קריאה של transcripts, credentials או `.env`.
- אין push ישיר ל־`main`.
- אין merge של PR #1 לפני שלוש מדידות baseline, review ודיווח.
- כל שינוי חייב להיות הפיך ב־PR אחד או revert אחד.
- אין לטעון לחיסכון לפני השוואה מלאה.

## מה יימדד
### יעילות
- זמן מתחילת session עד תחילת עבודה ממוקדת.
- זמן כולל עד תוצאה מאומתת.
- מספר קבצים שנקראו לפני שינוי.
- `/usage` לפני ואחרי המשימה, ככל שהפקודה זמינה.
- מצב context בתחילת המשימה ולאחר החקירה, אם זמין.
- מספר compact/clear/resume שנדרשו.

### איכות
- מספר סבבי תיקון.
- מספר דרישות שהוחמצו.
- מספר שינויים צדדיים או לא־נדרשים.
- static/build/preview שעברו.
- רגרסיות או תיקוני המשך לאחר PR.

### מעורבות יניב
- מספר שאלות הבהרה שהיה צורך לענות עליהן.
- מספר אישורים ידניים.
- זמן שבו יניב נדרש לעקוב אחרי התהליך.
- דירוג בהירות ואיכות בסולם 1–5.

## עיצוב הניסוי המתוקן
### שלב A — Baseline על המצב הישן
לבצע שלוש משימות רגילות מתוך checkout של `misparim/main` ב־SHA הקבוע:
1. P1 — שינוי תוכן/עיצוב קטן עם preview.
2. P2 — תיקון קוד בינוני בכמה קבצים עם typecheck/build/preview.
3. P3 — repository/bug investigation, read-only תחילה.

לכל משימה:
- session חדש.
- אותו מודל ו־effort שנבחרו למשימת ההשוואה המקבילה.
- מילוי `templates/BASELINE_SESSION_RECORD.md`.
- אין מעבר לענף מועמד השינוי במהלך אותה משימה.

### שלב B — מועמד שינוי שכבר הוכן
PR #1 כולל:
- קיצור `CURRENT_STATE.md` למצב חי בלבד.
- הסרת כפילות בין `CLAUDE.md`, `INDEX`, `USER_RULES` ו־`CHANGE_PROTOCOL`.
- שער כניסה תפעולי יחיד.
- שני path rules בלבד:
  - האתר הראשי.
  - `zaviyot/**/*`.
- אין שינוי בדרישות המוצר, בקוד, ב־DB או ב־assets.

לאחר השלמת baseline:
1. לקרוא את ה־diff המלא שוב.
2. לוודא שה־Rules אכן מתאימים לנתיבים.
3. לבדוק שאין חוזה מוצר שנעלם ממקורות האמת.
4. להחליט אם למזג את PR #1.

### שלב C — שלוש משימות post-change
רק לאחר merge מאושר, לבצע שלוש משימות דומות בגודל, תחום וסיכון ל־baseline, באותו מודל וב־effort דומה.

אין להשתמש באותה משימה בדיוק פעמיים, כדי לצמצם הטיית למידה; יש לבחור משימות מקבילות.

## תנאי הצלחה
הפיילוט ייחשב מוצלח רק אם מתקיימים יחד:
- ירידה של 20% לפחות בזמן ההבנה/פתיחה או בשימוש הנמדד, או שיפור ברור אחר שהוגדר מראש.
- אין עלייה במספר סבבי התיקון.
- אין דרישה קריטית שהוחמצה.
- אין רגרסיה חדשה.
- יניב מדווח שהשיחה ברורה וקלה יותר לניהול.

## תנאי עצירה
עוצרים ומחזירים לאחור אם:
- Claude מפספס חוזה מוצר בגלל מידע שלא נטען.
- נוצר מקור אמת נוסף או כפילות חדשה.
- יש עלייה ברורה בתיקונים או בשאלות הבהרה.
- path rules אינם נטענים באופן צפוי.
- אין שיפור מדיד אחרי שלוש משימות post-change.

## Rollback
- השינוי כולו נמצא ב־PR #1.
- `main` הישן נשמר כ־baseline עד החלטת merge.
- לאחר merge, rollback הוא revert של PR #1.
- אין לערב שינויי מוצר באותו PR.

## תוצרי הפיילוט
- שלושה רישומי baseline.
- review מתועד של PR #1.
- שלושה רישומי post-change, אם ה־PR מוזג.
- טבלת השוואה אחת.
- החלטה: keep, adjust או revert.
- רק אם בעיה עדיין חוזרת שלוש פעמים: בחינת Skill או Hook אחד.

## מקור שליטה
- מצב חי: `CURRENT_STATE.md`.
- ביקורת: `docs/REPOSITORY_AUDIT_2026-07-15.md`.
- checklist: Issue #12.
- תבנית מדידה: `templates/BASELINE_SESSION_RECORD.md`.
- PR מועמד: `yanivmizrachiy/misparim#1`.
